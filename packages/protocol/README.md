# Gateway/Node Protocol

Protocol version 1 defines envelopes for node authentication, heartbeats,
catalog queries, session commands and events, approvals, and user questions.
The transport is deferred; messages are transport-neutral JSON values.
