{
  "name": "dsh-channel-telegram-workspace",
  "version": "0.2.0",
  "private": true,
  "packageManager": "pnpm@11.7.0",
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "pnpm -r --if-present build",
    "typecheck": "pnpm --filter @dsh-channel-telegram/protocol build && pnpm --filter @dsh-channel-telegram/gateway build && pnpm -r --if-present typecheck",
    "test": "pnpm -r --if-present test",
    "check": "pnpm build && pnpm typecheck && pnpm test"
  },
  "devDependencies": {
    "@types/node": "^24.10.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  }
}
